# 车车的订阅转换

基于 `sub-web` 的前端订阅转换页面定制版。

当前调整：

- 移除广告信息弹窗
- 移除视频教程与下载页入口
- 移除 GitHub、Telegram、哔哩哔哩、YouTube 等宣传外链入口
- 前端后端地址固定为 `https://back.p-p.men`
- 新增 `Lanlan13-14/Rules` 规则分组，并默认使用标准版规则

## 启动

```bash
yarn install
yarn serve
```

## 构建

```bash
yarn build
```
